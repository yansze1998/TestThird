library flutter_application_third;

/// A Calculator.
class CalculatorThird {
  /// Returns [value] plus 3 nah.
  int addThree(int value) => value + 30;
}

class TestFunc {
  String getTestString() => "I also donno ?? updated!!";
}
