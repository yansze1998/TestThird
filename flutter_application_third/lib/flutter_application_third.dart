library flutter_application_third;

/// A Calculator.
class CalculatorThird {
  /// Returns [value] plus 3.
  int addThree(int value) => value + 3;
}

class TestFunc {
  String getTestString() => "I also donno";
}
